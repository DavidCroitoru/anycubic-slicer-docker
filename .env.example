# Build/host-side settings, read by docker-compose.yml only.
# Container-side settings live in runtime.env (see runtime.env.example).

# Empty = install whatever version is current in the Anycubic repo.
ASN_VERSION=

# Host directory exposed inside the slicer as /config/models.
MODELS_DIR=./models
